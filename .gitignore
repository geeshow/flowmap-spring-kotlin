# --- build artifacts ---------------------------------------------------------
build/
.gradle/
.kotlin/
*.class
out/

# --- IDE / local --------------------------------------------------------------
.idea/
.claude/
*.iml
.DS_Store

# --- local config -------------------------------------------------------------
# Machine-specific run config (absolute OUT_DIR etc.). The committed template is
# flowmap.config.example.
flowmap.config

# --- generated analysis output ------------------------------------------------
# Analysis output embeds analyzed-project internals (package names, file paths,
# resolved internal URLs) — never publish it. `refresh` writes into ./json/.
graph.json
*.graph.json
/json/

# --- analyzed source projects -------------------------------------------------
# .repo/ holds the code being analyzed (sample/demo + real projects). None of it
# is published — it is local analysis input, and real projects must never leak.
/.repo/

# (gradle wrapper jar IS tracked — do not add a global *.jar ignore)

# IDE/compiled class output
/bin/
